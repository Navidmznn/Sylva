# web
fastapi>=0.110,<0.120
uvicorn[standard]>=0.29,<0.40
python-multipart>=0.0.9
python-dotenv>=1.0,<2

# validation
pydantic>=2.5,<3
pikepdf>=8

# http client
httpx>=0.27,<0.29

# rate limiting
slowapi>=0.1.9
limits[redis]>=3.10

# database
SQLAlchemy>=2.0,<2.1
asyncpg>=0.29
psycopg[binary]>=3.1   # alembic only
alembic>=1.13

# job queue
arq>=0.26
redis>=5.0

# tokenizer (used by block.py for context-size estimation)
tiktoken>=0.7

# extraction — docling and torch only needed for the local branch
# pip install docling torch
