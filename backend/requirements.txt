# ── Web ───────────────────────────────────────────────────────────────────────
fastapi>=0.110,<0.120
uvicorn[standard]>=0.29,<0.40
python-multipart>=0.0.9

# ── Validation / models ───────────────────────────────────────────────────────
pydantic>=2.5,<3
pikepdf>=8

# ── HTTP client (Ollama) ──────────────────────────────────────────────────────
httpx>=0.27,<0.29

# ── Rate limiting (Redis-backed) ──────────────────────────────────────────────
slowapi>=0.1.9
limits[redis]>=3.10

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy>=2.0,<2.1
asyncpg>=0.29
psycopg[binary]>=3.1   # used by alembic only
alembic>=1.13

# ── Job queue ─────────────────────────────────────────────────────────────────
arq>=0.26
redis>=5.0

# ── Extraction (kept to ensure pip resolves consistently with worker) ─────────
# Docling and torch are not pinned here — the project already specifies them
# elsewhere. If you don't have them yet:
#   pip install docling torch
